-record(serv_st,{name,clients,channels}). %%channels är lista med namn på kanalerna
-record(cl_st, {          %% clients lista  alla möjliga klienter (records)
    gui, % atom of the GUI process
    nick, % nick/username of the client
    server % atom of the chat server
}).
-record(ch_st,{name,clients}). %% clients är en lista meddela
                                %% tupler, gui och namn på clienten
